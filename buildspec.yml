version: 1.0

phases:
  build:
    commands:
      - echo Nothing yet
    artifacts:
      files:
        - index.html
        - favicon.ico
        - resume.pdf
        - styles/*
        - images/*
