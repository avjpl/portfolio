version: 0.1

phases:
  pre_build:
    commands:
      - yarn
  build:
    commands:
      - yarn run webpack
artifacts:
  files:
    - index.html
    - favicon.ico
    - resume.pdf
    - styles/*
    - images/*
    - dist/bundle.js
